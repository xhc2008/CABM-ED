# 服务器配置示例
# 复制此文件为 .env 并根据需要修改

# 后端配置
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# 前端配置（Vite 需要 VITE_ 前缀）
VITE_FRONTEND_HOST=0.0.0.0
VITE_FRONTEND_PORT=3000

# API 地址（用于前端连接后端和加载图片）
# ⚠️ 重要：跨设备访问时必须修改此项！
# 
# 本地开发使用 localhost
VITE_API_URL=http://localhost:8000
# 
# 局域网访问时，将 localhost 改为服务器的局域网 IP，例如：
# VITE_API_URL=http://192.168.1.100:8000
# 
# 如何查看本机 IP：
# Windows: 运行 ipconfig 命令
# Mac/Linux: 运行 ifconfig 或 ip addr 命令
