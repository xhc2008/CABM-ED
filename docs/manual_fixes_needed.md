# 需要手动修复的内容

## 探索场景 (scenes/explore_scene.tscn)

由于场景文件的复杂性，需要手动在Godot编辑器中进行以下修改：

### 1. 移除旧的InventoryUI节点

在Godot编辑器中：
1. 打开 `scenes/explore_scene.tscn`
2. 在场景树中找到 `UI/InventoryUI` 节点
3. 右键点击并选择"删除"
4. 保存场景

**原因**：新的背包UI现在通过代码动态创建（在explore_scene.gd的_ready()函数中），不需要在场景中预先放置。

### 2. 确认InventoryButton节点存在

确保 `UI/InventoryButton` 节点仍然存在，它用于打开背包。

### 3. 测试步骤

完成上述修改后：

1. **运行探索场景**
   ```
   按F5运行游戏，进入探索场景
   ```

2. **测试背包UI**
   - 按B键打开背包
   - 确认UI全屏显示，不再堆在左上角
   - 确认三栏布局正确：左侧背包 | 中间空白 | 右侧详情
   - 按ESC或点击关闭按钮关闭背包

3. **测试宝箱功能**
   - 靠近宝箱
   - 按F键打开宝箱
   - 确认中间显示宝箱内容
   - 测试在背包和宝箱之间移动物品

## 主场景背包UI (scenes/inventory_ui.tscn)

主场景的背包UI场景文件需要更新为使用新的通用UI。

### 选项1：使用现有场景（推荐）

如果现有的 `inventory_ui.tscn` 布局已经正确，只需确保脚本正确：

1. 打开 `scenes/inventory_ui.tscn`
2. 选择根节点
3. 确认脚本为 `res://scripts/inventory_ui.gd`
4. 保存

### 选项2：替换为新场景

如果想使用全新的布局：

1. 在主场景中找到使用 `inventory_ui.tscn` 的地方
2. 替换为 `universal_inventory_ui.tscn`
3. 确保脚本设置为 `res://scripts/inventory_ui.gd`

## 验证清单

完成所有修改后，验证以下功能：

### 主场景
- [ ] 打开背包和仓库
- [ ] 在背包和仓库之间移动物品
- [ ] 物品堆叠正常工作
- [ ] 物品信息正确显示
- [ ] UI布局正确（三栏）

### 探索场景
- [ ] 按B键打开背包
- [ ] 背包UI全屏显示（不在左上角）
- [ ] 打开宝箱功能正常
- [ ] 在背包和宝箱之间移动物品
- [ ] 按ESC关闭背包
- [ ] 关闭背包后玩家控制恢复

## 如果遇到问题

### 问题1：背包UI仍然堆在左上角

**解决方案**：
1. 确认已删除场景中的旧InventoryUI节点
2. 确认explore_scene.gd中的代码正确创建新UI
3. 检查UniversalInventoryUI的_ready()函数是否正确设置锚点

### 问题2：找不到StorageContainer类

**解决方案**：
1. 确认 `scripts/storage_container.gd` 文件存在
2. 在Godot中：项目 -> 重新加载当前项目

### 问题3：物品图标不显示

**解决方案**：
1. 检查items.json配置文件中的icon路径
2. 确认图标文件存在于 `res://assets/images/items/` 目录

### 问题4：编译错误

**解决方案**：
1. 检查所有脚本的语法
2. 确认所有必需的文件都已创建
3. 重启Godot编辑器

## 需要的文件清单

确认以下文件都已创建：

- [x] `scripts/storage_container.gd`
- [x] `scripts/universal_inventory_ui.gd`
- [x] `scenes/universal_inventory_ui.tscn`
- [x] `scenes/explore_inventory_ui.tscn`
- [x] `scripts/inventory_manager.gd` (已更新)
- [x] `scripts/inventory_ui.gd` (已更新)
- [x] `scripts/explore/player_inventory.gd` (已更新)
- [x] `scripts/explore/explore_inventory_ui.gd` (已更新)
- [x] `scripts/inventory_slot.gd` (已更新)
- [x] `scripts/explore/explore_scene.gd` (已更新)

## 完成后

所有修改完成并测试通过后，可以删除以下旧文件（如果不再需要）：

- 旧的背包UI场景（如果有备份）
- 旧的脚本备份文件

重构完成！🎉
